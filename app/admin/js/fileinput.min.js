function imageUpload(input) {
	var imageInput = $(input);
	if (imageInput.data('id') > 0 ) {
		var preview = $('.image-preview[data-id=' + imageInput.data('id') + ']');
	} else {
		var preview = $('#image-preview');
	}

	var theImageFile = imageInput[0]['files'][0];

	if (theImageFile.type.indexOf('jpeg') > -1) {
		console.log('yes');

		var reader = new FileReader();

		reader.onloadend = function () {
			
			var img = new Image();
			img.crossOrigin = "anonymous";
			img.id = "hahaha";
			img.src = reader.result;

			var compressed = jic.compress(img, 76, 'jpg').src;

        	preview.attr('src', compressed);
        	alert('Image Is Compressed and resized');
       }

       if (theImageFile) {
           reader.readAsDataURL(theImageFile); //reads the data as a URL

       } else {
           preview.attr('src', "");
       }








	} else {
		console.log('no');
		alert('Only JPG allowed!');
	}

	var url = "imageupload.php";
}

function imageClick() {
	$('#imageInput').click();
}